<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yarmcfly.hae">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hae"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.webkit.WebSettings.TextSize"
            android:value="NORMAL" />
        <meta-data
            android:name="android.webkit.WebSettings.LayoutAlgorithm"
            android:value="NARROW_COLUMNS" />
        <meta-data
            android:name="android.webkit.WebSettings.BlockNetworkImage"
            android:value="false" />
        <meta-data
            android:name="android.webkit.WebSettings.UseWideViewPort"
            android:value="true" />
        <meta-data
            android:name="android.webkit.WebSettings.LoadWithOverviewMode"
            android:value="true" />

    </application>
</manifest>
